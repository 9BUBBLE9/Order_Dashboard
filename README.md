***Проблемы и решения:***
 - Identifier 'supabase' already declared в браузере - Переименовал переменную в supabaseClient, также думал,
что проблема в расширениях браузера.

 - Суммы заказов в RetailCRM были нулевыми - Добавил paymentType и items в запрос

 - RetailCRM не принимал дату в ISO-формате -	Исправил формат на Y-m-d H:i:s (добавил даты, чтобы график выглядил нормально, 
потому что в mock_orders.json нет дат и они генерируются уже в retailcrm, а там они генерируются с одной датой)

 - Telegram-бот отправлял повторные уведомления	- Добавил set() для хранения ID отправленных заказов

 - Telegram API был недоступен (блокировка в России) - Использовал прокси-сервер для обхода

***Примеры промптов:***
 - Напиши Python-скрипт, который выгружает заказы из RetailCRM через API и сохраняет их в Supabase.
 - Помоги настроить деплой статического дашборда на Vercel. Корень проекта — папка frontend,
 но Vercel ожидает index.html в корне репозитория. Как это правильно настроить через vercel.json?
 - При загрузке заказов в RetailCRM получаю ошибку "Invalid datetime". Формат ISO не подходит.
 Какой формат даты ожидает RetailCRM? Исправь код.
 - Бот отправляет повторные уведомления на одни и те же заказы. Как это исправить?
